# 富地房屋 — 第一版規格（MVP）

> 2026-09-13 建立。repo：`NightZhe/FudiHouse`（public），線上網址 `https://nightzhe.github.io/FudiHouse/`

## 目標
做一個類似 m.591.com.tw「買屋」頻道的**行動版優先**房產平台，給客戶瀏覽**富地房屋自家的出售物件**；
另有後台讓公司人員管理物件。第一版**只做出售**（不做租屋）。

## 架構（沿用 NightZhe/CarSocialMedia）
- React 19 + TypeScript + Vite 6 + react-router-dom + lucide-react + Tailwind CSS（npm 安裝，不用 CDN）
- 前台 / 後台同一個 SPA，用路由切開：前台 `/`、後台 `/admin`
- **部署在 GitHub Pages**：純靜態，所以用 `HashRouter`（避免子路徑重新整理 404），`vite.config.ts` 的 `base` 設 `/FudiHouse/`
- 資料存在瀏覽器 `localStorage`，首次載入用內建示範資料。
  資料存取集中在一個 repository 模組（例如 `src/services/listingRepository.ts`），
  之後換成真正的後端 API 只需要改這一層。
- GitHub Actions：push 到 `main` 自動 build 並部署到 Pages（`actions/upload-pages-artifact` + `actions/deploy-pages`）

### 已知限制（第一版刻意接受）
- 後台登入是**前端示範密碼**，公開 repo 看得到，不具安全性；正式上線要接後端認證。
- 後台的新增/編輯只存在「該瀏覽器」的 localStorage，不會同步給其他客戶。真正讓客戶看到後台改動需要後端。

## 品牌
- 名稱：**富地房屋**。需要一個 SVG logo（圖形標誌＋中文字標），放在前台頁首與後台側欄/頁首，也做成 favicon。
- 不可使用 591 的名稱、logo、配色識別；591 只是資訊架構參考。

## 資料模型 `Listing`（出售物件）
id、標題、社區名稱（可空）、縣市、行政區、路段地址（只到路/段，不寫門牌）、
總價（萬元）、權狀坪數、主建物坪數、單價（萬/坪，由總價÷權狀坪數計算，不手填）、
格局（房/廳/衛）、所在樓層/總樓層、屋齡（年）、型態（電梯大樓／華廈／公寓／透天厝／套房／店面）、
車位（無／坡道平面／機械／其他）、管理費（元/月，可空）、朝向（可空）、
特色標籤（陣列，例如 近捷運、有車位、邊間、新上架、降價）、描述、照片（陣列）、
聯絡人（姓名、電話、LINE ID）、狀態（上架／下架／已成交）、精選（布林）、瀏覽數、建立時間、更新時間。

示範資料 16–20 筆，分布在台北市、新北市、桃園市、台中市，價格/坪數/型態要合理且多樣（虛構物件）。
照片必須是實際存在、內容確實是住宅室內或建築外觀的圖片網址，建一張圖片對照表（key → 網址＋內容說明），物件引用 key。

## 前台（客戶）
| 路由 | 內容 |
|------|------|
| `/` 首頁 | 品牌頁首、搜尋列（關鍵字：標題/社區/地址）、縣市快捷、型態快捷分類、精選物件、最新上架 |
| `/list` 找房 | 篩選：縣市→行政區、總價區間、權狀坪數區間、房數、型態、屋齡；排序：最新／總價低→高／總價高→低／單價低→高／坪數大→小；顯示結果筆數；無結果空狀態 |
| `/house/:id` 物件詳情 | 照片輪播（可滑動、顯示 n/總數）、總價與單價、基本資料表、特色標籤、描述、Google 地圖連結（以路段地址搜尋）、收藏、聯絡按鈕（撥打電話 `tel:`、LINE）；進入詳情瀏覽數 +1 |
| `/favorites` 收藏 | 收藏清單（localStorage），空狀態 |

- 前台只顯示狀態為「上架」的物件。
- 底部分頁列：首頁／找房／收藏。
- 物件卡片：主圖、標題、行政區＋社區、格局｜權狀坪數｜樓層、總價（醒目）、單價、標籤。

## 後台（公司人員）
| 路由 | 內容 |
|------|------|
| `/admin` | 未登入顯示登入頁（示範密碼，登入狀態存 sessionStorage）；登入後進儀表板 |
| 儀表板 | 物件總數、上架／下架／已成交數、總瀏覽數、上架物件平均單價 |
| 物件管理 | 列表（縮圖、標題、總價、狀態、瀏覽數）、關鍵字搜尋、狀態篩選、切換狀態、編輯、刪除（需二次確認） |
| 新增／編輯物件 | 表單涵蓋資料模型所有欄位；必填驗證（標題、縣市、行政區、總價、權狀坪數）；單價即時計算顯示；照片可貼網址增刪 |
| 其他 | 登出；「重置示範資料」（需二次確認） |

- 後台的新增/編輯/刪除/狀態變更，回到前台（同一瀏覽器）要立即反映。

## 視覺與體驗
- 行動版優先（iPhone 寬度 375–430px 為主），桌機寬度內容置中、不要拉成滿版長條。
- 要有房產品牌的質感：沉穩可信賴，不要廉價模板感、不要 AI 味的漸層與過度圓角陰影。
- 價格用千分位與「萬」單位，數字對齊清楚。
- 觸控目標 ≥ 44px；有 loading／空狀態；尊重 `prefers-reduced-motion`。

## 驗收條件
1. `npm ci && npm run build` 成功，無 TypeScript 錯誤。
2. `npm run preview` 後，前台四個路由與後台所有頁面都能開，重新整理不會 404（HashRouter）。
3. 前台只出現「上架」物件；篩選與排序結果正確（抽 3 組條件人工核對）。
4. 詳情頁：輪播可切換、單價＝總價÷權狀坪數（四捨五入到小數 1 位）、`tel:` 連結正確、瀏覽數會增加。
5. 收藏：加入/移除後重新整理仍保留。
6. 後台：錯誤密碼無法登入；新增一筆上架物件後前台看得到；改成下架後前台消失；刪除需確認；必填欄位空白無法送出。
7. 375px 寬度下無水平捲動、文字不溢出；logo 在頁首與 favicon 正常顯示。
8. 所有示範照片網址實際可載入（HTTP 200），內容是住宅相關。
9. `.github/workflows/deploy.yml` 存在且設定正確（build 產物為 `dist`，base 路徑正確）。
10. README.md 說明：功能、技術棧、本地開發指令、後台示範密碼、部署方式、已知限制。
